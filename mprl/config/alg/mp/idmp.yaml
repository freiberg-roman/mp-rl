mp_type: "prodmp"

tau: 0.20
num_basis: ${env.idmp.num_basis}
num_dof: ${env.num_dof}
device: ${device}

mp_args:
  alpha_phase: 1.0
  num_basis: ${env.idmp.num_basis}
  basis_bandwidth_factor: ${env.idmp.basis_bandwidth_factor}
  num_basis_outside: 0
  dt: ${env.dt}
  alpha: 20
  weight_scale: 1000.0
  goals_scale: 10.0