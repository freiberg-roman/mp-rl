defaults:
  - _self_
  - alg: sac
  - buffer: settings
  - env: half_cheetah
  - eval: scheme
  - logging: wandb
  - prediction: ground_truth
  - train: scheme

root_dir: "../experiments"
device: "cpu"
project_name: "sac_mixed_mp_tau_025"
run_id: 0

hydra:
  run:
    dir: ${root_dir}/${alg.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ${root_dir}/${alg.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}

