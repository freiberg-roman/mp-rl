defaults:
  - _self_
  - env: reacher
  - agent: mp_time_sac
  - ctrl: pd
  - mp: idmp
  - train: off_policy
  - buffer: sequence_buffer

root_dir: "../experiments"
mode: "train_sac"
device: "cpu"
use_wandb: False
run: 0

hydra:
  run:
    dir: ${root_dir}/${mode}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ${root_dir}/${mode}/${now:%Y-%m-%d}/${now:%H-%M-%S}

