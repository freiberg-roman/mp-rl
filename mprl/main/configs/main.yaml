defaults:
  - _self_
  - agent: ssac
  - buffer: sequence_buffer
  - ctrl: pd
  - env: half_cheetah
  - mp: promp
  - train: off_policy

root_dir: "../experiments"
mode: "train_mprl"
device: "cpu"

hydra:
  run:
    dir: ${root_dir}/${mode}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ${root_dir}/${mode}/${now:%Y-%m-%d}/${now:%H-%M-%S}

