name: "idmp"

alpha: 10
tau: 4.0
num_basis: ${env.idmp.num_basis}
num_dof: ${env.num_dof}
device: ${device}

mp_args:
  alpha_phase: 10.0
  num_basis: ${env.idmp.num_basis}
  basis_bandwidth_factor: ${env.idmp.basis_bandwidth_factor}
  num_basis_outside: 0
  dt: ${env.dt}
  alpha: 10